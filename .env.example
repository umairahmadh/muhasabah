# Copy to .env and change this. This is the single password to log in.
MUHASABAH_PASSWORD=changeme

# Where the SQLite file lives. Defaults to ./muhasabah.db next to the app.
# MUHASABAH_DB=./muhasabah.db
